🍃 MongoDB CRUD OPERATIONS (ATLAS)
What is MongoDB (1-line refresher)

MongoDB is a NoSQL, document-based database that stores data in JSON-like documents (BSON) instead of tables.

Database ➝ Collection ➝ Document
(SQL: Database ➝ Table ➝ Row)

🌍 What is MongoDB Atlas?

MongoDB Atlas is MongoDB’s cloud database service.

It provides:

Free cloud clusters

Automatic backups

Web UI (Data Explorer)

Easy connection with apps

📌 You don’t install MongoDB locally — Atlas hosts it.

🧱 Basic Structure Example

Database: college
Collection: students

Sample document:

{
  "_id": ObjectId("..."),
  "name": "Asha",
  "age": 20,
  "course": "CS",
  "marks": 85
}

🔹 insertOne — CREATE
📌 What does it do?

Inserts one document into a collection.

✅ Syntax
db.collection.insertOne(document)

🔹 Example
db.students.insertOne({
  name: "Asha",
  age: 20,
  course: "CS",
  marks: 85
})


✔ MongoDB automatically creates:

_id (unique identifier)

🧠 Exam line

insertOne() is used to insert a single document into a MongoDB collection.

🔍 find — READ
📌 What does it do?

Retrieves documents from a collection.

✅ Syntax
db.collection.find(query)

🔹 Find all documents
db.students.find()

🔹 Find with condition

Students from CS course:

db.students.find({ course: "CS" })

🔹 Find with multiple conditions
db.students.find({ course: "CS", age: 20 })

🔹 Pretty output (Atlas / shell)
db.students.find().pretty()

🧠 Exam line

find() is used to retrieve documents from a MongoDB collection based on a query.

✏️ updateOne — UPDATE
📌 What does it do?

Updates only the first document that matches the condition.

✅ Syntax
db.collection.updateOne(
  filter,
  update
)

🔹 Example: Update marks
db.students.updateOne(
  { name: "Asha" },
  { $set: { marks: 90 } }
)


✔ $set is mandatory
❌ Without $set, whole document is replaced

🔹 Increment value
db.students.updateOne(
  { name: "Asha" },
  { $inc: { marks: 5 } }
)

🧠 Exam line

updateOne() updates the first matching document using update operators like $set.

🗑️ deleteOne — DELETE
📌 What does it do?

Deletes only one document that matches the condition.

✅ Syntax
db.collection.deleteOne(filter)

🔹 Example
db.students.deleteOne({ name: "Asha" })

⚠️ Danger case
db.students.deleteOne({})


❌ Deletes any one random document

🧠 Exam line

deleteOne() removes the first document that matches the filter condition.

🔁 CRUD SUMMARY TABLE (VERY IMPORTANT)
Operation	Method	Description
Create	insertOne	Add new document
Read	find	Retrieve documents
Update	updateOne	Modify one document
Delete	deleteOne	Remove one document
🌐 How this looks in MongoDB Atlas

In Atlas Data Explorer:

Click Database

Select Collection

Use:

Insert Document

Filter

Update

Delete

Same logic, UI based.

❗ COMMON MISTAKES (EXAM + PRACTICAL)

Forgetting $set in update

Using findOne() vs find() confusion

Empty filter in delete

Assuming schema is fixed (MongoDB is schema-less)

🧠 One-line interview summary

MongoDB CRUD operations allow inserting, retrieving, updating, and deleting documents from collections using simple JSON-based commands.